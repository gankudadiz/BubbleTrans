# v2.4 Qt-Material 主题升级

**日期**：2026-07-06
**关联设计**：[v2.4 Qt-Material 主题升级 PDR](../../docs/01_项目规划与设计/2026-07-06_v2.4_Qt-Material主题升级_PDR.md)

## 状态：已完成（2026-07-06）

## 实施结果
- 集成 qt-material 库，`src/main.py` 加一行 `apply_stylesheet()` 全局应用 Material Design 主题
- 通过 `app.styleHints().colorScheme()` 自动跟随系统明暗模式
- `requirements.txt` 新增 `qt-material>=2.14`，`setup_check.py` 补检测逻辑保证开箱即用
- 后续交互优化（commit d1cde86）：侧边栏可拖拽、工具栏双行布局、预翻译按钮反馈、清除缓存下拉菜单
- 关联提交：d8d6772, d1cde86

## 执行步骤

1. `pip install qt-material`
2. 在 `src/main.py` 加入 `apply_stylesheet()` 调用
3. 启动验证：主窗口、设置弹窗、QMessageBox 主题统一
4. 切换 3~5 个配色方案，选定最终主题
5. 检查内联 QSS 冲突，移除冲突样式
6. 更新 `README.md` 版本号 → v2.4

## 验收清单

- [x] A1 主题统一：主窗口、设置弹窗、弹窗均为深色 Material 主题
- [x] A2 按钮精致：圆角 + hover 变色 + press 反馈
- [x] A3 输入框精致：圆角边框 + focus 高亮
- [x] A4 下拉框精致：下拉列表统一深色
- [x] A5 滚动条精致：自定义深色样式
- [x] A6 选中反馈：文件列表选中项高亮明显
- [x] A7 功能无损：翻译全流程正常
- [x] A8 密度合理：信息密度不过于稀疏
